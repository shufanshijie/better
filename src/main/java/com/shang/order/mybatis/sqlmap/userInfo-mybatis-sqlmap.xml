<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shang.order.domain.UserInfoMapper">

	<resultMap type="com.shang.order.domain.UserInfoDO" id="UserInfoDOResultMap">
    	<result column="id" property="id"/>
    	<result column="user_name" property="userName"/>
    	<result column="user_code" property="userCode"/>
	</resultMap>

	<sql id="MybatisUserInfoDAO_all_column_fields">
	      id,user_name,user_code
 	</sql>
 	
	<!-- 只有varchar char text类型 做 !='' 判断，生成代码时,其他类型时只做!=null 判断 0或fasle与空''是相同的，在mybatis中的动态语句中 -->
	<sql id="MybatisUserInfoDAO_dynamic_where_fields"><!-- xml转义字符需要 <![CDATA[   ]]> 标签-->
		<where>
				<if test="id != null "> AND id=#{id} </if>
				<if test="userName != null  and userName != '' "> AND user_name=#{userName} </if>
				<if test="userCode != null  and userCode != '' "> AND user_code=#{userCode} </if>
		</where>
 	</sql>
 	





 	<select id="MybatisUserInfoDAO_select_dynamic" resultMap="UserInfoDOResultMap" parameterType="com.shang.order.domain.UserInfoDO">
	     SELECT
	     	<include refid="MybatisUserInfoDAO_all_column_fields" />
		 FROM
		 	user_info
			<include refid="MybatisUserInfoDAO_dynamic_where_fields" />
 	</select>

 	<select id="MybatisUserInfoDAO_select_dynamic_count" resultType="long" parameterType="com.shang.order.domain.UserInfoDO">
	     SELECT
	     	count(1)
		 FROM
		 	user_info
	 	<include refid="MybatisUserInfoDAO_dynamic_where_fields" />
 	</select>

 	<select id="MybatisUserInfoDAO_select_dynamic_page_query" resultMap="UserInfoDOResultMap" parameterType="com.shang.order.domain.UserInfoDO">
	     SELECT
	     	<include refid="MybatisUserInfoDAO_all_column_fields" />
		 FROM
		 	user_info
	 	<include refid="MybatisUserInfoDAO_dynamic_where_fields" />
	 	Limit #{start},#{pageSize}
 	</select>

</mapper>